// The layout renders the denial message for users off the allow-list; this
// page exists only as the redirect target from requireReportsUser().
export default function NoAccessPage() {
  return null;
}
